'use client';

import ChatContainer from '@/app/chat/ChatContainer';

export default function ChatPage() {
  return (
    <section className="min-h-screen max-w-screen from-gray-100 to-gray-200 flex justify-center items-start">
      <ChatContainer />
    </section>
  );
}
